import { ICommand } from 'wokcommands'

export default {
    name: '',
    category: '',
    description: '',
    slash: 'both',
    guildOnly: true,

    callback: async ({ }) => {

    },
} as ICommand