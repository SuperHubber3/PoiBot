import { ICommand } from 'wokcommands'

export default {
    name: '',
    category: '',
    description: '',
    slash: 'both',
    guildOnly: true,
    testOnly: true,

    callback: async ({ }) => {

    },
} as ICommand